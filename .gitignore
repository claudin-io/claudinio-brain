/target
*.db
*.db-wal
*.db-shm
.brain/

# The f32 table is the download; only the int8 quantization is compiled in.
# 30 MB of redundant weights has no business in the history.
models/**/model.safetensors
